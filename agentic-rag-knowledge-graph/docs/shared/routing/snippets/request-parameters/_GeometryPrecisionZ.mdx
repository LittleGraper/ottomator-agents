import ParamMetadata from "../../../components/param-metatdata" 

<ParamMetadata types={[{name:"integer"}]} defaultValue="3" />

{/* <p class="space-x-2"><span class="font-sm text-color-3">type:int</span><span class="font-bold text-[#6d29cc]">default:`3`</span></p> */}

Specify the number of decimal places that will be used in the response geometries returned by the solve operation. This applies to z-values only (not x-, y-, or m-values).

```javascript
geometryPrecisionZ = 3
```