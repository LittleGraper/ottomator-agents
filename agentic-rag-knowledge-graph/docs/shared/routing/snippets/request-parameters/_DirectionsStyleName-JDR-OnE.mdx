{/*Shared snippet for directions_style_name OR directionsStyleName */} 

import ParamMetadata from "../../../components/param-metatdata" 

<ParamMetadata types={[{name:"string"}]} defaultValue="NA Desktop" />

{/* <p class="space-x-2"><span class="font-sm text-color-3">type:enum</span><span class="font-bold text-[#6d29cc]">default:`NA Desktop`</span></p> */}

<If c="publication=='rest/online' && requestType=='direct'">
**Allowed values:** `NA Desktop`, `NA Navigation`, `NA Campus`
</If>

<If c="publication=='rest/online' && requestType=='job'">
**Allowed values:** `NA Desktop`, `NA Navigation`
</If>

<If c="publication=='services-reference/enterprise'">
**Allowed values:** `NA Desktop`, `NA Navigation`, `NA Campus`
</If>

Specify the name of the formatting style for the directions. This parameter can be specified using the following values:

- `NA Desktop`—Generates turn-by-turn directions suitable for printing.
- `NA Navigation`—Generates turn-by-turn directions designed for an in-vehicle navigation device.
<If c="publication=='rest/online' && requestType=='direct' ">
- `NA Campus`—Generates turn-by-turn walking directions designed for pedestrian routes.
</If>
<If c="publication=='services-reference/enterprise'">
- `NA Campus`—Generates turn-by-turn walking directions designed for pedestrian routes.
</If>


<If c="requestType=='job'">
This parameter applies only when the [`populate_directions`](#populate_directions) parameter is set to `true`. 
</If>

<If c="requestType=='direct'">
This parameter applies only when the [`returnDirections`](#returndirections) parameter is set to `true`. 
</If>
