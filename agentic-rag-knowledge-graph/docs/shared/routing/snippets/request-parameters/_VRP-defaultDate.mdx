import ParamMetadata from "../../../components/param-metatdata" 

<ParamMetadata types={[{name:"datetime",link:"../routing-data-types#datetime"}]} />

{/* <p class="space-x-2"><span class="font-sm text-color-3">type:string</span><a class="font-sm text-color-3" href="../routing-data-types#datetime">datetime</a></p> */}

Specify the date on which all the routes start.

The parameter value should be specified as a numeric value representing the milliseconds since midnight, January 1, 1970.

Only the date portion of this value will be used; the time-specific portion of the value will be ignored.

You can use the same value that you have specified for the `EarliestStartTime` attribute for each route feature in the [`routes`](#routes) parameter. If your routes start on different dates, you can pick any date from one of them.