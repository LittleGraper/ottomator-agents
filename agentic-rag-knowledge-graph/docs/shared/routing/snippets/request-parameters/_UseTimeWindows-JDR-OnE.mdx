{/*Shared snippet for useTimeWindows OR use_time_windows */} 

import ParamMetadata from "../../../components/param-metatdata" 

<ParamMetadata types={[{name:"boolean"}]} defaultValue="false" />

{/* <p class="space-x-2"><span class="font-sm text-color-3">type:bool</span><span class="font-bold text-[#6d29cc]">default:`false`</span></p> */}

Specify whether the service will consider time windows specified on the stops when finding the best route. The time windows are specified on [stops](#stops) using the `TimeWindowStart` and `TimeWindowEnd` attributes.