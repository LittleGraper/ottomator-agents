{/* f: Metadata type, chip, and default values */}

import ParamMetadata from "../components/param-metatdata"

<If c={!!props.f}>

<ParamMetadata required types={[{name:"string"}]} values={"json | pjson"}/>

The response format.

```
f=json
```

</If>

{/* Token: Metadata type, chip, and default values */}

<If c={!!props.token}>

<ParamMetadata required types={[{name:"string"}]} />

An <Term term="access token">access token</Term> with the required privileges. 

* ArcGIS Location Platform: <i>{props.alpPrivileges}</i>
* ArcGIS Online: <i>{props.agoPrivileges}</i>

```
token=<ACCESS_TOKEN>
```

<MoreInfo labelMore="HTTP headers for tokens">

To use HTTP headers _instead_ of the `token` parameter, set the following:
 
<CodeSwitcher>
<CodeBlock language="html" label=" X-Esri-Authorization">{`
GET <SERVICE_REQUEST> HTTP/1.1
  Host: <SERVICE_DOMAIN>
  X-Esri-Authorization: Bearer <ACCESS_TOKEN>
`}</CodeBlock>
<CodeBlock language="html" label="Authorization">{`
GET <SERVICE_REQUEST> HTTP/1.1
    Host: <SERVICE_DOMAIN>
    Authorization: Bearer <ACCESS_TOKEN>
`}</CodeBlock>
</CodeSwitcher>

</MoreInfo>

Learn more about <a href="xref://site.documentation/access-tokens">access tokens</a> and <a href="xref://site.documentation/privileges">privileges</a> in the <i>Security and authentication</i> developer guide.

</If>