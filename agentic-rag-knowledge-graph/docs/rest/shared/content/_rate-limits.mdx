<If c={!!props.geocoding}>
The rate limits for the geocoding service are...
</If>
<If c={!!props.routing}>
The rate limits for the routing service are...
</If>
<If c={!!props.geoenrichment}>
The rate limits for the GeoEnrichment service are 100 requests/s.
</If>