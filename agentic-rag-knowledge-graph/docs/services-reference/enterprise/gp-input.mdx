---
title: "Input parameters"
uid: "gp-input"
description: "The geoprocessing inputs  resource provides the value of an input parameter of an asynchronous job."
slug: "gp-input"
url: "/gp-input"
guid: "GUID-1EA1CD85-85A2-4AAC-A2E3-F4D497845AE8"
migratedTopicMetadata:
  FTITLE: "GP Input"
  FDESCRIPTION: "inputs for a succeeded async job"
  FCHANGES: "New title and regular updates"
  FISHRELEASELABEL: ""
  FESRISOFTWARERELEASE: ""
  FESRITAGCLOUD: ""
  FESRIRECREATIONPROCEDURE: ""
  FESRIPRODUCTLIFECYCLE: "11.1 - *"
  CREATED-ON: "22/12/2022 14:28:13"
  FAUTHOR: "ssuo"
  FSTATUS: "Released"
  MODIFIED-ON: "01/02/2023 11:34:02"
  VERSION: "5"
  FRESOLUTION: ""
  DOC-LANGUAGE: "en"
  FISHREVCOUNTER: "8"
  ED: "GUID-54ACBAED-53DB-4637-A1F9-624A069B6675"
  FUSERGROUP: "VUSERGROUPARCGISONLINEDOC"
  READ-ACCESS: ""
restInfoMetadata:
  apiPath: /inputs/<param-name>
  urlSegments:
    - name: <job-url>
      url: /gp-job
    - name: /inputs/<param-name>
  methods:
    - get
    - post
  sslOnly: 'no'
  versionIntroduced: '9.3'
  relatedResources:
    - name: Inputs
      url: /inputs
    - name: Jobs
      url: /gp-job
---

## Description

The geoprocessing `input` resource represents an input parameter for an asynchronous geoprocessing job. It provides the name, data type, and value for an input parameter. See the [`jobs`](/gp-job) resource for more information about an asynchronous geoprocessing job.

The value of the input parameter is the value provided when [a job is submitted](/submit-gp-job). See [Geoprocessing services data types](/gp-data-types) for the syntax for an input parameter.

If there are many input parameters to query, you can also use the [Inputs](/inputs) resources, available from ArcGIS Enterprise 11.1.

##  Request parameter

For a job in succeeded status, the input parameter URL is available from the [`jobs`](/gp-job) resource. You can specify the following parameter for the response format:

<StyledTable headers={"Parameter,Details"}>
<StyledTableRow>
<StyledTableCell cellStyle="markdown">

`f`

(Optional)

</StyledTableCell>
<StyledTableCell cellStyle="markdown">

Description: The response format. The default is `html`.

Values: `html` | `json` | `pjson` | `kmz`

</StyledTableCell>
</StyledTableRow>
</StyledTable>

For a job that is not in succeeded status, such as a canceled or failed job, no link or relative URL is available from the `jobs` resource; however, you can construct the request manually in the format `https://machine.domain.com/webadaptor/rest/services/<ServiceName>/GPServer/<TaskName>/jobs/<jobID>/inputs/<InputParameterName>`. The request parameter above still applies.

## Request example

For a `MailingList` task job, you can access its `SearchDistance_ft` input parameter with the following URL:

Obtain the input for the job with a jobID of `jdb4cce48c464424e913c15c4f419b32a`.

```markdown
https://machine.domain.com/webadaptor/rest/services/Portland/ESRI_CadastralData_Portland/GPServer/MailingList/jobs/jdb4cce48c464424e913c15c4f419b32a/inputs/SearchDistance_ft
```

 

## JSON Response syntax

The JSON Response includes the name of the parameter, the data type, and its value. The syntax of the `value` field can vary based on the [data type](/gp-input) of the parameter.

The response always has three keys: `paramName`, `dataType`, and `value`.

```json
{
    "paramName" : "<parameter name>",
    "dataType" : "<data type>",
    "value" : <literal or object>
}
```

 

## JSON Response example

The input parameter `Input_String` with the `GPString` data type, and the value of `TestString`.

```json
{
    "paramName" : "Input_String",
    "dataType" : "GPString",
    "value" : "TestString"
}
```

 
