---
title: "Generate Token"
description: "This operation generates an ArcGIS token that can be used by clients when working with the ArcGIS Notebook Server Administrator API."
slug: "generate-token"
url: "/generate-token"
guid: "GUID-48B796CA-481C-4A99-89BD-39FC2591532C"
migratedTopicMetadata:
  FTITLE: "Generate Token"
  FDESCRIPTION: ""
  FCHANGES: ""
  FISHRELEASELABEL: ""
  FESRISOFTWARERELEASE: ""
  FESRITAGCLOUD: ""
  FESRIRECREATIONPROCEDURE: ""
  FESRIPRODUCTLIFECYCLE: "10.7 - *"
  CREATED-ON: "18/10/2018 13:37:43"
  FAUTHOR: "smacdonald"
  FSTATUS: "Released"
  MODIFIED-ON: "07/02/2019 15:27:10"
  VERSION: "1"
  FRESOLUTION: ""
  DOC-LANGUAGE: "en"
  FISHREVCOUNTER: "5"
  ED: "GUID-C2E0422E-E0CE-43B2-8390-FD623E64D3D1"
  FUSERGROUP: "VUSERGROUPSERVERDOC"
  READ-ACCESS: ""
restInfoMetadata:
  apiPath: /generateToken
  urlSegments:
    - name: <notebookserveradmin>
      url: /notebook-server-administration-root
    - name: /generateToken
  methods: []
  sslOnly: 'no'
  versionIntroduced: '10.7'
---

## Description

This operation generates an ArcGIS token that can be used by clients when working with the ArcGIS Notebook Server Administrator API.

##  Request parameters

<StyledTable headers={"Parameter,Details"}>

<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`username` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The case-sensitive username of the user who wants to get a token.

Example

```javascript
username=testAdmin89
```

 

</StyledTableCell>

</StyledTableRow>

<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`password` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The password of the user who wants to get a token.

Example

```javascript
password=test1234
```

 

</StyledTableCell>

</StyledTableRow>

<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`client` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The client identification type for which the token is to be generated. Users can specify whether the token will be generated for a client application's base URL, a user-specified IP address, or the IP address that is making the request.

Values: `referer`  \| `ip`  \| `requestip` 

</StyledTableCell>

</StyledTableRow>

<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`referer` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The base URL of the web app that will invoke the request to access a secured resource. This parameter must be specified if the value of the `client`  parameter is `referer` .

Example

```javascript
referer=https://myserver/mywebapp
```

 

</StyledTableCell>

</StyledTableRow>

<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`ip` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The IP address of the machine that will invoke the request to access a secured resource. This parameter must be specified if the value of the `client`  parameter is `ip` .

Example

```javascript
ip=###.###.###.###
```

 

</StyledTableCell>

</StyledTableRow>

<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`expiration` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The token expiration time in minutes. The default is 60 minutes (one hour). The maximum value of the expiration time is controlled by the server. Requests for tokens larger than this time will return a token for the maximum allowed expiration time. Applications are responsible for renewing expired tokens; expired tokens will be rejected by the server on subsequent requests that use the token.

Example

```javascript
expiration=60
```

 

</StyledTableCell>

</StyledTableRow>

<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`f` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The response format. The default response format is `html` .

Values: `html`  \| `json`  \| `pjson` 

</StyledTableCell>

</StyledTableRow>

</StyledTable>
