---
title: "Validate a Webhook Receiver"
description: "Allows administrators to validate a service webhook receiver."
slug: "validate-webhook-receiver"
url: "/validate-webhook-receiver"
restInfoMetadata:
  apiPath: /notebooks/webhookReceivers/validate
  urlSegments:
    - name: <root>
      url: /notebook-server-administration-root
    - name: /notebooks/webhookReceivers/<webhook receiver id>/validate
  methods:
    - get
  requiredCapability: 'Administrator'  
  sslOnly: 'no'
  versionIntroduced: '11.4'
---

## Description

The validate webhook receiver operation allows administrators to validate a service webhook receiver registered with ArcGIS Notebook Server. 

## Request parameters

<StyledTable headers={"Parameter,Details"}>
    <StyledTableRow>
        <StyledTableCell cellStyle="markdown">

            `f`

        </StyledTableCell>
        <StyledTableCell cellStyle="markdown">

            The response format. The default is `html`.


            Values: `html` | `json` | `pjson`

        </StyledTableCell>
    </StyledTableRow>
</StyledTable>

## JSON Response syntax

```other
{
    "success": "true|false",
    "id": <webhook_receiver_id>
}
```

## JSON Response example

```other
{
    "success": "true",
    "id": "212e9839-3cb8-4666-a312-073b00e59f7f"
}
```
