---
title: "Delete User Workspace"
description: "Enables an administrator to remove a user's workspace directory permanently"
slug: "delete-user-workspace"
url: "/delete-user-workspace"
restInfoMetadata:
  apiPath: /dataaccess/<workspacename>/deleteUserWorkspace

  urlSegments:
    - name: <root>
      url: /notebook-server-administration-root
    - name: /dataaccess/<workspacename>/deleteUserWorkspace
  methods:
    - post
  sslOnly: 'no'
  versionIntroduced: '11.4'
  requiredCapability: 'Administrator'
---

## Description

The `deleteUserWorkspaces` operation enables an administrator to remove a user's workspace directory permanently.


<Note>


The `Delete User Workspaces` resource can also be accessed by the `/dataaccess/deleteUserWorkspaces` endpoint. 

</Note>

## Request parameters

<StyledTable headers={"Parameter,Details"}>
<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`userName` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The `userName` of the user whose notebook workspace directory is being deleted.

</StyledTableCell>

</StyledTableRow>
<StyledTableRow>

<StyledTableCell cellStyle="markdown">

`f` 

</StyledTableCell>

<StyledTableCell cellStyle="markdown">

The response format. The default response format is `html` .

Values: `html`  \| `json`  \| `pjson` 

</StyledTableCell>

</StyledTableRow>
</StyledTable>

## Example usage

Below is a sample request URL and parameters for the `deleteUserWorkspaces` operation:


```
HTTP POST
https://notebookserver.domain.com/arcgis/admin/dataaccess/deleteUserWorkspace
userName=gis_joe
f=json
```

## JSON Response syntax
```
{"status": "<success | failure>"}

```
## JSON Response example
### Example 1:
```
{"status":"success"}
```

### Example 2 - Error when source user has one or more active containers:
```
{
  "error" : {
    "code" : 500,
    "message" : "Failed to delete user workspace. The user has actively running containers.",
    "details" : null
  }
}
```
