---
title: "Close Notebook (Notebook)"
description: "This operation stops a running notebook."
slug: "close-notebook"
url: "/close-notebook"
restInfoMetadata:
  apiPath: notebooks/<notebookID>/closeNotebook
  urlSegments:
    - name: <root>
      url: /enterprise-admin-root
    - name: notebooks/<notebookID>/closeNotebook
  methods:
    - post
  sslOnly: 'no'
  versionIntroduced: '11.4'
  requiredCapabilities:
    - name: Access allowed with "Create and edit notebooks" privilege
      url: /managing-access-to-the-enterprise-admin-api
---

## Description

The `closeNotebook` operation stops a running notebook.

Idle notebooks are automatically cleaned up according to the duration defined by the `idleNotebookThreshold` property in [Notebook Configurations](/notebook-configurations). The default value for that property is 12 hours.

## Request parameters

<StyledTable headers={"Parameter,Details"}>
  <StyledTableRow>
    <StyledTableCell cellStyle="markdown">

      `f` 

    </StyledTableCell>
    <StyledTableCell cellStyle="markdown">

      The response format. The default response format is `html`.

      Values: `html` | `json` | `pjson`

    </StyledTableCell>
  </StyledTableRow>
</StyledTable>


## Example usage

The following is a sample POST request for the `closeNotebook` operation.

```other
POST /context/admin/notebooks/7f64ace427684e90a8397a5e476a4190/closeNotebook HTTP/1.1
Host: organization.domain.com
Content-Type: application/x-www-form-urlencoded
Content-Length: [ ]

f=pjson&token=<token>

```

## JSON Response syntax

```json
{
  "status": "<success | failure>"
}
```

## JSON Response example

```json
{
  "status": "success"
}
```