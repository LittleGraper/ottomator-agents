---
title: "Manifest Status"
description: "This resource returns whether the deployment is using an imported version manifest."
slug: "manifest-status"
url: "/manifest-status"
restInfoMetadata:
  apiPath: /system/upgrades/manifestStatus
  urlSegments:
    - name: <root>
      url: /enterprise-admin-root
    - name: /system/upgrades/manifestStatus
  methods:
    - get
  sslOnly: 'no'
  versionIntroduced: '11.4'
---

## Description

The `manifestStatus` resource returns whether the deployment is using an [imported version manifest](/import-manifest) or the Esri provided version manifest.

## Request parameters

<StyledTable headers={"Parameter,Details"}>
  <StyledTableRow>
    <StyledTableCell cellStyle="markdown">

      `f`

    </StyledTableCell>
    <StyledTableCell cellStyle="markdown">

      The response format. The default format is `html`.

      Values: `html` | `json` | `pjson`

    </StyledTableCell>
  </StyledTableRow>
</StyledTable>


## Example usage

The following is a sample request URL used to access the `manifestStatus` resource:

```other
https://organization.domain.com/context/admin/system/upgrades/manifestStatus?f=pjson

```


## JSON Response examples

<Tabs>

<TabNav>
  <TabTitle>Esri provided</TabTitle>
  <TabTitle>Imported</TabTitle>
</TabNav>

<Tab>

The response below is returned when the deployment is using the Esri provided version manifest:

```other
{
  "versionManifestSource": "Esri provided",
  "lastImportedTimestamp": 0
}

```
</Tab>

<Tab>

The response below is returned when the deployment is using an imported version manifest, including the timestamp of when the version manifest was imported:

```other
{
  "versionManifestSource": "Imported",
  "lastImportedTimestamp": 1729168213605
}

```
</Tab>

</Tabs>