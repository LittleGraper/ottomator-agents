---
title: "/removeFromServer: Remove from Server"
description: "Removes a selected data store from a server."
slug: "remove-from-server"
url: "/remove-from-server"
guid: "GUID-679B639D-4498-4F9E-8250-AA3EBACF76CE"
migratedTopicMetadata:
  FTITLE: "Remove from server"
  FDESCRIPTION: ""
  FCHANGES: "Title, URL, and example usage changes"
  FISHRELEASELABEL: ""
  FESRISOFTWARERELEASE: ""
  FESRITAGCLOUD: ""
  FESRIRECREATIONPROCEDURE: ""
  FESRIPRODUCTLIFECYCLE: "10.9-*"
  CREATED-ON: "01/03/2021 13:24:07"
  FAUTHOR: "tohara"
  FSTATUS: "Released"
  MODIFIED-ON: "25/08/2021 10:53:32"
  VERSION: "2"
  FRESOLUTION: ""
  DOC-LANGUAGE: "en"
  FISHREVCOUNTER: "4"
  ED: "GUID-8BB58491-3A49-4000-8FF0-ED3E3AB00F3A"
  FUSERGROUP: "VUSERGROUPARCGISONLINEDOC"
  READ-ACCESS: ""
restInfoMetadata:
  apiPath: /portals/[portalID]/datastores/removeFromServer
  urlSegments:
    - name: '[root]'
      url: /root
    - name: /portals/[portalID]/datastores/removeFromServer
  methods:
    - post
  sslOnly: 'no'
  versionIntroduced: 10.7.1
---

## Example usage

The following is a sample ArcGIS Enterprise POST request for the `removeFromServer` operation:

```markdown
POST /webadaptor/sharing/rest/portals/0123456789ABCDEF/datastores/removeFromServer HTTP/1.1
Host: machine.domain.com
Content-Type: application/x-www-form-urlencoded
Content-Length: []

datastoreId=55d87f404a7e420bb969ccb70254bfec&serverId=9sQswqEpB31ymiCD&f=pjson

```

## Description

The `removeFromServer` operation unregisters a specified data store from your ArcGIS Server. A data store cannot be removed, however, if there are outstanding bulk-publishing layers on the specified server. All layers published through the [Publish Layers](/publish-layers) operation must be deleted before the data store can be unregistered.


##  Request parameters

<StyledTable headers={"Parameter,Details"}>
  <StyledTableRow>
    <StyledTableCell cellStyle="markdown">

      `datastoreId` 

      (Required)

    </StyledTableCell>
    <StyledTableCell cellStyle="markdown">

      The `itemId` of the data store you want to remove from the given server. Note that a single data store may be registered across multiple servers. Use the [getServers](/get-servers-for-data-store) operation to determine if the data store is registered across multiple servers.

      Example

      ```markdown
      datastoreId=55d87f404a7e420bb969ccb70254bfec
      ```

    </StyledTableCell>
  </StyledTableRow>
  <StyledTableRow>
    <StyledTableCell cellStyle="markdown">

      `serverId` 

      (Required)

    </StyledTableCell>
    <StyledTableCell cellStyle="markdown">

      The `serverId` of the server you want to remove the data store from.

      Example

      ```markdown
      serverId=9sQswqEpB31ymiCD
      ```

    </StyledTableCell>
  </StyledTableRow>
  <StyledTableRow>
    <StyledTableCell cellStyle="markdown">

      `f` 

    </StyledTableCell>
    <StyledTableCell cellStyle="markdown">

      The response format. The default format is `html`.

      Values: `html` | `json` | `pjson`

    </StyledTableCell>
  </StyledTableRow>
</StyledTable>


## JSON Response examples

If successful, `removeFromServer` will return the following success message:

```other
{"success": true}
```

If the operation fails, `removeFromServer` will return an error similar to the message below:

```other
{
  "error": {
    "code": 400,
    "message": "Remove data store from server failed.",
    "details": [
      "Unable to find server with specified id."
    ]
  }
}
```